name: Code Lint and Pre-Commit
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:


jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    container: python:3.11-buster

    steps:
      - uses: actions/checkout@v3

      - name: Install Poetry
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: 1.5.1

      - name: Install Dependencies
        run: poetry install

      - name: Install Pre-Commit
        run: poetry run pre-commit uninstall && poetry run pre-commit install

      - name: Lint
        run: poetry run task lint
